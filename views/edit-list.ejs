<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit <%= list.title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main class="container">
    <h1>Edit List</h1>

    <form action="/lists/<%= list._id %>/update" method="POST" class="form-card">
      <label for="title">List Name</label>
      <input
        type="text"
        id="title"
        name="title"
        value="<%= list.title %>"
        required
      >

      <button type="submit">Save Changes</button>
    </form>

    <p>
      <a href="/lists/<%= list._id %>">← Back to this list</a>
    </p>
  </main>
</body>
</html>
